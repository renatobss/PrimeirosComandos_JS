function recursiva(inicio, fim) {
    for (let i = inicio; i <= fim; i++) {
        console.log(i)        
    }
}
recursiva(12, 20)


//Crie uma função recursiva que recebe um número inicial e um limite, e imprime todos os valores até chegar ao limite.